# Gaia data files — assumed pre-downloaded, not committed (~800MB)
Data/

# IRIS / webgateway container runtime state
durable/compose/
webgateway/durable/
*.csv
*.csv.gz
# but DO track the tiny committed CI fixture (real data stays ignored above)
!tests/fixtures/*.csv.gz

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
